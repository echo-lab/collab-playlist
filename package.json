{
  "name": "collab-playlist",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "set NODE_ENV=development && cd client && start npm run dev-build-watch && cd .. && nodemon server.js --watch server.js",
    "prod-start": "set NODE_ENV=production && echo \"make sure to npm run build\" && node server.js",
    "build": "cd client && npm run build",
    "dev-build": "echo \"feel free to terminate the watch after build finishes\" && cd client && npm run dev-build-watch",
    "setup-mods": "cd setup_mods && dev-build-setup.bat",
    "reset-mods": "cd client && npm i --save react-scripts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "react-scripts": "^3.2.0"
  }
}
